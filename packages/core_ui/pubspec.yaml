name: core_ui
description: Shared design system for Aura Marketplace apps
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.13.0
  flutter: ">=3.19.0"

resolution: workspace

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^6.0.0
